    // import React from 'react';
    // import axios from 'axios';

    // const DeleteProduct = ({ productId, onDelete }) => {
    //     const handleDelete = async () => {
    //         const apiUrl = `https://vica.website/api/items/${productId}`;

    //         try {
    //             const response = await axios.delete(apiUrl);
    //             if (response.status === 200) {
    //                 alert('تم حذف المنتج بنجاح!');
    //                 onDelete(productId); // استدعاء onDelete بعد نجاح الحذف
    //             } else {
    //                 alert('حدث خطأ أثناء حذف المنتج!');
    //             }
    //         } catch (error) {
    //             console.error('خطأ أثناء الاتصال بالـ API:', error);
    //             alert('لا يمكن إتمام العملية. يرجى المحاولة لاحقًا.');
    //         }
    //     };

    //     return (
    //         <button onClick={handleDelete}>حذف المنتج</button>
    //     );
    // };

    // export default DeleteProduct;
